// src/domain/port/out/{{pascalName}}RepositoryPort.ts
// Add domain-specific methods only — avoid generic CRUD.
// Examples: findById(id: {{pascalName}}Id): Promise<{{pascalName}} | null>
//           save(entity: {{pascalName}}): Promise<void>
export interface {{pascalName}}RepositoryPort {
}
