// src/domain/port/out/{{pascalName}}RepositoryPort.ts
// Add domain-specific methods only — avoid generic CRUD.
// Examples: findOpen{{pascalName}}sForCustomer(customerId: CustomerId): Promise<{{pascalName}}[]>
//           mark{{pascalName}}AsProcessed({{camelName}}Id: {{pascalName}}Id): Promise<void>
export interface {{pascalName}}RepositoryPort {
}
