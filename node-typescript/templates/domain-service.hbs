// Pure domain service: shared business logic used by multiple use cases.
// Zero dependencies on frameworks, infrastructure, or application code.
// All methods must be pure functions — no I/O, no logging, no side effects.
export class {{pascalName}}Service {
  // Replace with domain-specific method signatures.
  {{camelName}}(input: {{pascalName}}Input): {{pascalName}}Output {
    throw new Error('Not implemented');
  }
}
